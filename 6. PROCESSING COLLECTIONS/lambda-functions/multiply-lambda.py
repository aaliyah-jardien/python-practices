# STEP 2 Multiply Lambda
# Now define a simple lambda that takes parameters x and y and return x * y
lambda x,y: x*y

# Print out the result from calling the lambda
print((lambda x,y: x*y)(5,3)) # :) 1st try
