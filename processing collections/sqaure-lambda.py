# STEP 1 Square Lambda
# Define a simple lambda that takes parameter x and multiplies it with itself
lambda x: x*x

# Print out the result from calling the lambda with x of 9
print((lambda x: x*x)(9)) # 9 is the value of x
